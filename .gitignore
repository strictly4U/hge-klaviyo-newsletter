# Build artifacts
*.zip
build/
dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Composer (if added later)
/vendor/
composer.lock

# Node (if added later for build pipelines)
/node_modules/
package-lock.json

# PHPUnit cache (if added)
.phpunit.result.cache
.phpunit.cache/

# OS
Thumbs.db
desktop.ini

# Internal-only docs — task tracking, staging runbooks, planning notes.
# Kept on disk for local use but not committed to either the monorepo or
# the public distribution repo. See bin/sync-plugin-to-public.sh for the
# matching rsync --exclude list.
TESTING.md
TODO.md
PLAN*.md
TASKS*.md
.beads/
