# Build artifacts
*.zip
build/
dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Composer (if added later)
/vendor/
composer.lock

# Node (if added later for build pipelines)
/node_modules/
package-lock.json

# PHPUnit cache (if added)
.phpunit.result.cache
.phpunit.cache/

# OS
Thumbs.db
desktop.ini

# Internal-only docs (sync from monorepo deliberately excludes these — they
# live in the private development workspace, not in this public distribution
# repo). Defensive gitignore so an accidental cp / rsync doesn't reintroduce.
TESTING.md
TODO.md
PLAN*.md
TASKS*.md
.beads/

